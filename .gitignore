# .gitignore for the PMSTopTen project

# ignore Mac-specific directory descriptor file
.DS_Store
# ignore Eclipse file
.project
 # ignore the top level GeneratedFiles, but don't ignore the GeneratedFiles in Historical/
/Generated*/
# ignore any directory named 'old' - temp backups
old/
# ignore vi temp files
*.swp
# DON'T ignore anything in the Historical directory
!/Historical/*
# BUT...DO ignore .DS_Store in Historical/
/Historical/.DS_Store
# ignore SourceData that we fetch to compute Top Ten numbers:
/SourceData-*/*
properties_DB*.txt
GetResults.pl.launch
Generated-*/Support
# don't save any of the RSIDN files - they contain personal information that we must not
# store on a public repository
*[rR][sS][iI][nN][dD]*
*[rR][sS][iI][dD][nN]*
